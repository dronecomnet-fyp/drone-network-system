# /etc/NetworkManager/conf.d/unmanaged-dtn.conf (file 01 step 5)
# wlan1 is configured deterministically by dtn-net.service (iw + static
# IP); NetworkManager must not touch it.
[keyfile]
unmanaged-devices=interface-name:wlan1
